# Evento

## Database_Read_Write :
- To find if the customer/product exists, used the `findOne()` mongoose method in customerRoute.js and productRoute.js
- To update the values, used the `findByIdAndUpdate()` mongoose method in customerRoute.js and productRoute.js
- Used `save()` mongoose method in customerRoute.js
